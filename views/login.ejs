<!DOCTYPE html>
<html>
  <head>
    <title>Pinterest | Log-In</title>
    <link rel="stylesheet" href="../stylesheets/style.css" />
  </head>
  <body>
    <div class="css-selector">
      <div class="container">
        <div class="content">
          <img
            src="https://i.pinimg.com/originals/d3/d1/75/d3d175e560ae133f1ed5cd4ec173751a.png"
            alt="pin logo"
            class="img1"
          />
          <p class="header">Welcome back</p>
          <% if(error.length > 0) { %>
          <p class="error"><%= error %></p>
          <% } %>
          <form action="/login" method="post">
            <input
              name="username"
              type="text"
              placeholder="Username"
              class="detail"
            />
            <br />
            <input
              name="password"
              type="password"
              placeholder="Password"
              class="detail"
            />
            <br />
            <input type="submit" class="btn int" value="Log In" />
            <a href="/">Don't have an account? Make one </a>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
